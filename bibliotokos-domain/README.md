# This is a HUMAN ONLY document.
## AI is NOT permitted to write to it

Flow:

1. build services/ bins
2. create a database with the admin tool
3. setup a verified user with the admin tool, declaring them admin
4. run the server against the databse created (set by .env)
5. build frontend/
6. run the frontend, configured with the port of the server for login/logout/jwt verification